[project]
name = "network"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "transformers",
    "onnx",
    "onnxsim",
    "onnxruntime>=1.24.4",
    "onnxscript>=0.7.0",
    "ezkl",
]
